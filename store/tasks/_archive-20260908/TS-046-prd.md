# TS-046 · PRD — End-to-end wiring: the panel shows live data

> Generated by spec · attached 2026-08-24 · fan-out 3 of 3 from the eng.integration session.

## 1 · Problem
Every piece is green in isolation and nothing works together. The last task's suite must not test the panel or the endpoint — it must test **the thing the person asked for** in the original sentence: "I need a scroll animation for the landing page."

## 2 · Proposal
Wire the task surface end to end: the panel renders live data from a cold chat room, no stubs, no reload required for an open task; the cost cell reads "not measured" (never $0) until the ledger link exists; `changes_requested` writes history.

## 3 · Working agents
- **mia** (doer, front half) · **quinn** (doer, back half)
- **quinn** (verifier) — the e2e spec is the suite; assertions ≠ authors
- **operator** (approver)

## 4 · Departments
Engineering.

## 5 · Constraints
- The deferral stays visible: "not measured", never $0.
- The whole flow must work from a cold chat room — chat → session → task → panel.

## 6 · Acceptance criteria
1. Panel renders live data, not fixtures
2. Cost cell reads "not measured", never $0
3. An open task refreshes without a reload
4. `changes_requested` writes history
5. Whole flow works from a cold chat room

## 7 · Context refs
- `dashboard/app/chat/page.tsx` — the wiring point
- `dashboard/tests/e2e-panel.spec.ts` — new, the suite
- `dashboard/app/api/task-spec/route.ts` — the read side

## 8 · RICE
**6.1** — reasoning-based, not formula-verified. [backlog-rules rule 0.6]
