# TS-042 · PRD — Scroll animation for the landing page

> Generated by spec · attached 2026-08-19 · **inherited by TS-043** (revision, forward-only).

## 1 · Problem
The landing page has no motion. Generated video failed seam continuity at 3s (evidence: 2 probes, $0.40 — both frames drifted 14° at the seam; a third probe would test the same failing property and was refused).

## 2 · Proposal
A programmatic SVG trace — one line entering, branching to three departments, rejoining. ~4 KB vector, no drift, no regeneration when copy changes. Lives in the CAOS panel inside the 312px chat rail.

## 3 · Working agents
- **mia** (doer) — design.motion, owns the panel
- **quinn** (verifier) — owns the browser suite; assertions ≠ author
- **operator** (approver) — the one human gate

## 4 · Departments
Engineering (design.motion surface), Brand Studio (tokens — Adora violet/paper/hairline).

## 5 · Constraints
- Hard generation ceiling: **$0.50 strict** — refused providers cannot be priced; unaffordable providers are refused before any spend.
- Keyboard reachable at every control; no measurement rendered that was not taken.

## 6 · Acceptance criteria
1. Renders inside the 312px rail with no horizontal overflow
2. Every step row expands to show its decision
3. No measurement rendered that was not taken
4. Keyboard reachable at every control

> These four lines ARE the suite's assertions — one list, three renderings (PRD / task / run).

## 7 · Context refs
- `docs/ORCHESTRATION.md` — the Work Ladder
- `Teams/Engineering/mia/agent.md` — the doer's operating contract

## 8 · RICE
**0 — unranked**, `scripts/rice.py` did not run for this item (reasoning-based, not formula-verified). [backlog-rules rule 0.6]
