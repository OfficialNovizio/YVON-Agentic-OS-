# TS-044 · PRD — Data contract: designSessionId on TaskSpecItem

> Generated by spec · attached 2026-08-23 · fan-out 1 of 3 from the eng.integration session.

## 1 · Problem
`designSessionId` is read at three call sites in `TaskFocusView.tsx` and is not on the `TaskSpecItem` interface. It works at runtime only because the route spreads `...t` straight from `task.py list`. That is a type hole, not an interface.

## 2 · Proposal
Add `designSessionId` (and every field the panel reads) to the `TaskSpecItem` type in `dashboard/app/chat/TasksPanel.tsx` and align the response shape in `dashboard/app/api/task-spec/route.ts`.

## 3 · Working agents
- **dev** (doer) — engineering lead, owns the contract
- **quinn** (verifier) — tsc is the assertion
- **operator** (approver)

## 4 · Departments
Engineering.

## 5 · Constraints
- Cheapest-to-reject-first ordering inside the fan-out: a type definition is hours of work and unblocks two multi-day tasks.

## 6 · Acceptance criteria
1. `designSessionId` is on `TaskSpecItem` — `tsc --noEmit` passes with 0 errors
2. Every field the panel reads is declared

## 7 · Context refs
- `dashboard/app/chat/TaskFocusView.tsx` — the three call sites
- `dashboard/app/api/task-spec/route.ts` — the response shape
- `dashboard/app/chat/TasksPanel.tsx` — the interface

## 8 · RICE
**4.5** — reasoning-based, not formula-verified. [backlog-rules rule 0.6]
