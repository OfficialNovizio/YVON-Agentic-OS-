# TASK-SPEC template v3 (2026-08-24) — generated by meta/task-dispatch. Do not fill by hand
# without meta's protocol: discovery once → DAG → sign-off → activate.
#
# v3 adds (from the "One Request, End to End" artifact, beats 9–22):
#   · review state            — gated → review → done; the suite decides (task.sh review / suite)
#   · blocked SIDECAR         — blocked: true keeps the status field unchanged; a task can be
#                               blocked AND executing (task.sh block / unblock)
#   · acceptance statuses     — per-criterion pass/fail/not_run/pending/deferred + evidence
#                               (task.sh set-acceptance); flat-string criteria still parse (legacy)
#   · doer/verifier/integrator— per work item (task.sh set-roles)
#   · handoff packet          — entry/contract/stubbed/needs_wiring/tokens/verified_on
#                               (task.sh set-handoff → event handoff_emitted)
#   · derived_from            — distinct from revision_of: same goal (revision) vs different
#                               goal made possible by the first (derived). new --derived-from
#   · superseded_by           — auto-marked on `new --revision-of` (rotation is forward-only)
#   · run_ref                 — the run record replaces prose exit proof (task.sh suite --run)
#   · updated_at              — stamped on every transition/activity (staleness)
id: TS-049
status: executing            # draft → discovery → approved → executing → gated → review → done
source_message: "Build the Novizio editorial dashboard

Build and verify an original, responsive Novizio dashboard in the linked repository using the verified COS study as the structural and visual reference. Reinterpret the reference for Novizio with original content, branding, and imagery; document Brunello Cucinelli as blocked site-level and use it only as directional context."
requester: operator
rules:
  sharding: worker receives ONLY its work item + consumed contracts
  handoff: contracts-only — no transcript inheritance
  inject_form: .toon (this YAML is the source form)

classification:
  task_type: ""
  departments: []
  lead: "mia"
  # gate_0: true              # uncomment if this touches frontend/backend/API/security/algorithm structure (MASTER Sec7.0) — requires gate_0_signoffs before approve
  # gate_0_signoffs: []       #   who signed the RFC (dev/spec/meta/warden, or [operator] if operator-ordered)

context:                 # hook-injected, gate-verified (citations only)
  chunks: []
  existing_assets: []
  conflicts: []

discovery:               # BLOCKING — no fan-out until resolved
  questions: []
  decisions:
    - "Working agent: mia (lead); quinn for QA + responsive verification; dev for architecture review"
    - "Reference: the verified COS study is the structural and visual reference; build an original Novizio reinterpretation"
    - "Brunello Cucinelli is blocked at site level - directional context only, no direct content or assets"
    - "Scope: original responsive editorial dashboard in the linked repository (desktop/tablet/mobile)"
    - "No backend, APIs, data persistence, or analytics work - explicitly out of scope"

work_items:
  - id: WI-1
    owner: "mia"
    objective: "Build and verify an original, responsive Novizio editorial dashboard in the linked repository using the verified COS study as the structural and visual reference."
    strategy: BALANCE 1.0x   # per-item budget (FAST | BALANCE ×)
    consumes: []
    produces: "dashboard/public/novizio/index.html"
    owns_paths: []
    skills: []
    scripts: []
    blocked_by: []
    # v3: one entry per acceptance criterion. status ∈ pass|fail|not_run|pending|deferred.
    # Evidence = the run detail ("chromium 1300×1000 · pass") — a criterion and an
    # assertion are the same line. Flat strings ("- \"criterion text\"") still parse as
    # status: pending for legacy records.
    acceptance:
      - text: "Dashboard availability: a reviewer can reach and inspect the Novizio editorial dashboard in the delivered work"
        status: pending
        evidence: ""
      - text: "Responsive behavior: usable and editorially coherent at desktop, tablet, and mobile viewports with no clipped content, unusable controls, overlap, or horizontal overflow"
        status: pending
        evidence: ""
      - text: "Novizio originality: Novizio-specific branding, editorial content, and imagery only — no COS or Brunello branding, content, or assets presented as Novizio material"
        status: pending
        evidence: ""
      - text: "Reference reinterpretation: applies the verified COS study's structural/visual principles without reproducing its identity or exact presentation"
        status: pending
        evidence: ""
      - text: "Blocked-source handling: documentation explicitly identifies Brunello Cucinelli as blocked at site level, directional context only"
        status: pending
        evidence: ""
      - text: "Verification quality: a reviewer can confirm responsive, visual, content, branding, imagery, and blocked-source requirements without undocumented assumptions"
        status: pending
        evidence: ""
    security_review: none
    # v3 roles: doer (writes), verifier (owns the suite — assertions ≠ author),
    # integrator (takes the brief at handoff). Defaults to owner when unset.
    doer: "mia"
    verifier: "quinn"
    integrator: "dev"

dag:
  parallel: []
  critical_path: []

exit_gate:
  owner: ""
  proof: ""

feedback:                # filled post-execution — anneal consumes
  outcome: null          # accepted | revised | rejected
  lesson: null

created_at: "2026-09-05T08:20:56+00:00"
updated_at: "2026-09-05T17:27:03+00:00"
revision_of: null         # set at creation if this is a retry/redo of another task (task.sh new --revision-of <TS-id>)
derived_from: null        # v3 — set if this task was made possible by another (task.sh new --derived-from <TS-id>)
superseded_by: null       # v3 — set automatically when a revision of this task is created (forward-only rotation)
blocked: false            # v3 SIDECAR — true while blocked; status field is UNCHANGED (task.sh block/unblock)
blocked_at: ""            # v3
blocked_reason: ""        # v3 — never empty while blocked: true
run_ref: ""               # v3 — the run record that closed review/done (task.sh suite --run <path>)
history:
  - {ts: "2026-09-05T08:20:56+00:00", actor: "novy738@yvon.internal", event: "opened_draft", note: ""}

  - {ts: "2026-09-05T08:24:14+00:00", actor: "spec", event: "prd_attached", note: "ref=store/tasks/TS-049-prd.md rice=0.2"}
  - {ts: "2026-09-05T08:25:47+00:00", actor: "spec", event: "discovery_filled", note: "lead=mia; 5 decision(s)"}
  - {ts: "2026-09-05T08:25:59+00:00", actor: "spec", event: "discovery_opened", note: ""}
  - {ts: "2026-09-05T08:25:59+00:00", actor: "operator", event: "approved", note: ""}
  - {ts: "2026-09-05T08:25:59+00:00", actor: "operator", event: "executing_started", note: "1 work item(s) dispatched"}
  - {ts: "2026-09-05T08:28:51+00:00", actor: "operator", event: "evidence_attached", note: "image: COS women full-page screenshot <https://hermes.yvon.in/artifacts/novizio/6e5ffbd9-015e-4013-9841-2ad8cfdb713f/cos-women-fullpage.png>"}
  - {ts: "2026-09-05T08:28:51+00:00", actor: "operator", event: "evidence_attached", note: "file: COS design spec (design.md) <https://hermes.yvon.in/artifacts/novizio/6e5ffbd9-015e-4013-9841-2ad8cfdb713f/design.md>"}
  - {ts: "2026-09-05T08:28:52+00:00", actor: "operator", event: "evidence_attached", note: "data: COS scraped reference text <https://hermes.yvon.in/artifacts/novizio/6e5ffbd9-015e-4013-9841-2ad8cfdb713f/reference.md>"}
  - {ts: "2026-09-05T17:27:03+00:00", actor: "operator", event: "roles_set", note: "WI-1: doer=mia, verifier=quinn, integrator=dev"}
# Handoff packet (v3, task.sh set-handoff) — written while the context is still live.
# Six fields: entry / contract / stubbed / needs_wiring / tokens / verified_on.
handoff: {}

# PRD gate (2026-08-18, docs/PRD-prd-gated-task-conversion.md) — BLOCKING on
# discovery -> approved (task.sh approve refuses without both). Set together
# via `task.sh set-prd <id> --ref <path> --rice <score>`, never hand-typed.
prd_ref: "store/tasks/TS-049-prd.md"
rice_score: "0.2"

# Evidence block (2026-09-04, evidence rail fix ⑥) — what this task was built
# ON: screenshots/scraped data the chat turn actually saved (the wrapper's
# https /artifacts/ store). Appended per-URL via
# `task.sh set-evidence <id> --url <u> --label <name> --kind image|data|file`,
# carried automatically from the chat proposal's artifacts[] on PRD
# conversion. Descriptive, never a gate; absent/[] on tasks without evidence.
evidence:
  - url: "https://hermes.yvon.in/artifacts/novizio/6e5ffbd9-015e-4013-9841-2ad8cfdb713f/cos-women-fullpage.png"
    label: "COS women full-page screenshot"
    kind: "image"

  - url: "https://hermes.yvon.in/artifacts/novizio/6e5ffbd9-015e-4013-9841-2ad8cfdb713f/design.md"
    label: "COS design spec (design.md)"
    kind: "file"
  - url: "https://hermes.yvon.in/artifacts/novizio/6e5ffbd9-015e-4013-9841-2ad8cfdb713f/reference.md"
    label: "COS scraped reference text"
    kind: "data"
# Design-origin linkage (2026-08-19, docs/PRD-design-first-workflow.md) —
# optional, only set on tasks that came out of cli/design.py's handoff, via
# `task.sh set-design-origin <id> --session <sid> --tool screenshot-to-code|open-design|custom
#   [--artifact <artifact_id>] [--handoff <path>]`. Never hand-typed. Powers
# the dashboard's unified design-preview panel (TaskFocusView) — absent on
# every task not sourced this way, never guessed.
design_session_id: ""      # store/design-sessions/{id}.json
design_tool: ""            # screenshot-to-code | open-design | custom
design_artifact_id: ""     # open-design live-artifact id, when one exists
design_handoff_path: ""    # store/design-sessions/{id}-handoff.md

approved_by: operator
approved_at: 2026-09-05T08:25:59+00:00
