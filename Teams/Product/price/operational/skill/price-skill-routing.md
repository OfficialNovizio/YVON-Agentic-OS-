# price — Skill Routing Map

Non-leader: law (the Evidence Gate — price runs it for monetization) + this routing decide WHICH skill WHEN.

```
new price / no WTP evidence ─► pricing-research (WTP by segment; value-anchored; flagged methods)
                                        │ WTP structure
                                        ▼
                              packaging-tiers (value metric; good/better/best; feature→tier; fences)
                                        │ structure
                                        ▼
                    pricing-experiment-discipline (loom's rigor + blast-radius rules; new/holdout only)
                                        │ won test
                                        ▼
                    price-change-governance (impact; grandfather; board if locked commitment) ─► ship ─► metric reads revenue+churn
```

Handoffs out: loom (revenue experiments, registry) · metric (revenue-per-user, churn instrumentation) · board/precedent (locked-commitment changes, Fleet Charter Rail 3) · echo/comms (change communication) · felix/Finance/future (margin/unit-economics boundary) · spec (feature-to-tier seam) · mia/Engineering (pricing-page build) · anneal (a mispriced-and-missed outcome → skill lesson).
Precedence: research proposes, revenue experiments confirm; existing customers never silently repriced; locked commitments → board; every change is a logged proposal.
