# Vaultwarden config — copy to `.env` in this folder and fill. NEVER commit `.env`.
VW_DOMAIN=https://vault.<FILL_IN: your-domain>
# Generate a strong admin token:  openssl rand -base64 48
VW_ADMIN_TOKEN=<FILL_IN: openssl rand -base64 48>
