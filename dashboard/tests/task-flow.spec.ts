// STALE — superseded 2026-08-24 by tests/e2e/task-flow.spec.ts (smoke) and
// tests/e2e/e2e-panel.spec.ts (full suite).
//
// playwright.config.ts's testDir is ./tests/e2e, so a spec in the tests/ root
// is a gate that never runs. The live files live in tests/e2e/. Delete this
// once a terminal is available — it contains no tests.
