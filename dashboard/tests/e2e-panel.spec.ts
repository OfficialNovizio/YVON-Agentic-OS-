// STALE — superseded 2026-08-24 by tests/e2e/e2e-panel.spec.ts.
//
// playwright.config.ts's testDir is ./tests/e2e, so a spec in the tests/ root
// is a gate that never runs — the exact "silent skip" the repo's rules forbid.
// The live file is tests/e2e/e2e-panel.spec.ts (the file TS-046's record names).
// Delete this file once a terminal is available (or keep as a tombstone — it
// contains no tests either way).
