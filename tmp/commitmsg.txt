Clear all chats, tasks and design sessions

Operator requested a full reset ahead of a new approach.

  - chat_rooms: 37 active archived via archived_at (0 active remain of 58 total)
  - design sessions: 6 files (3 sessions x .json + -design.md) moved to
    store/design-sessions/_archive-<timestamp>/
  - tasks: already down to TEMPLATE.yaml

Archived rather than hard-deleted so nothing is unrecoverable. Supabase rows and
the events ledger still exist and are queryable.

NOT touched: the captured reference bundles and generated artifacts under
/opt/yvon-hermes-http/workspaces/_artifacts/ - those are expensive to reproduce
(a capture is ~10 minutes plus tokens), so they are left in place pending an
explicit instruction.