Clear chat + task state ahead of a fresh reference-clone run

Archived rather than deleted, matching the operator's earlier cleanup pattern:
- 7 task YAMLs/MDs -> store/tasks/_archive-<date>/ (TEMPLATE.yaml kept)
- 4 design sessions -> store/design-sessions/_archive-<date>/
- store/tasks/.active marker cleared
- 21 Supabase chat_rooms soft-deleted via archived_at (not hard-deleted, so the
  history stays queryable); one fresh kind='thread' room created.

Stack verified ready: dashboard localhost:3000 -> 200, hermes healthz ok,
capture-worker polling, dev server up.