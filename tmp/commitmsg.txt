Never re-ask an answered clone-vs-adapt gate

Third fix in the offforum hijack chain. Sequence of what was actually wrong:
  1. 'convert to task' released the frame to lena          -> fixed (continuation hold)
  2. [GATE DECISION] released the frame to lena            -> fixed (system marker hold)
  3. the intent gate was RE-PRESENTED after being answered -> this commit

The design session already stores `intent` (mode + decidedAt), so the decision
was durable all along - the route just did not consult it. The model re-emits the
intent fence on later turns, and the fence parser turned every one of those into a
fresh gate card, so the user was asked IDENTICAL CLONE / ADAPT again after already
choosing. The intent branch of the fence parser now requires !session.intent;
when intent is recorded the branch is skipped, gatePayload stays null and the card
is suppressed.

Also confirmed while tracing: captureProgress WAS being set correctly from the
capture.progress SSE frames (page.tsx:935). The progress bar was rendering
OFF-SCREEN, hidden by the unbounded card stack fixed in ca57122 - so the scroll
fix should also restore the visible scraping progress bar. No separate change made.

Still open and NOT claimed as done: 'convert to task' should act instead of
writing a long message; the reference-capture card is not collapsible; turn
duration under governor throttling.