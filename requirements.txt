# YVON RAG Pipeline — Python Dependencies
# Install: pip install -r requirements.txt --break-system-packages

# Embedding
sentence-transformers>=2.2.0
scikit-learn>=1.0.0

# Vector store
sqlite-vec>=0.1.0

# PDF processing (optional, for chunkify.py)
pypdf>=3.0.0
pdfplumber>=0.7.0

# Document -> Markdown conversion for book ingestion (rag/scripts/add_book.py).
# Handles docx/pptx/xlsx/odt/ods/odp/rtf/epub/csv/pdf in one call, benchmarked
# above pypdf/pdfplumber/pandoc/markitdown for quality — github.com/firecrawl/anydoc
firecrawl-anydoc>=0.1.9

# CLI tools
numpy>=1.24.0

# cli/design.py's live generate_code() call to a self-hosted screenshot-to-code
# instance — the real endpoint is a WebSocket (docs/PRD-design-first-workflow.md,
# cli/lib/screenshot_to_code_client.py's header). Only needed for live generation;
# capture and stub mode don't require it.
websockets>=14.0

# Sandbox-first quarantine (Part 7.7 Tier-2) — SDK confirmed installable +
# unit-tested 2026-08-10 (pip install opensandbox opensandbox-cli); live
# container provisioning still needs a Docker/K8s runtime, absent here.
opensandbox==0.1.14
opensandbox-cli==0.1.1

# scrapegraphai REMOVED 2026-09-07 (operator order): the LLM-driven scraper
# burned inference tokens on every page and had zero runtime callers; the
# former target_companies enrichment need is covered by crawl4ai + the agent
# LLM reading its markdown. Removal notes:
#   Teams/Shared OS/tools/shared-tool-registry.md
# (Historical, for context only: the PyPI 1.76.0 release was broken with
# langchain-community>=0.4.0 — ChatOllama symbol moved — so it was installed
# from GitHub main with --ignore-requires-python. Do not reinstall without
# re-opening the registry row.)
